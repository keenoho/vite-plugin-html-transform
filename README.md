# vite-plugin-html-transform

## usage

vite.config.js

```
import htmlTransfromPlugin from './vite-plugin-html-transform';

export default {
    ...other config

    plugins: [
      htmlTransfromPlugin(),
      ...other plugin
    ],

    ...other config
}
```
